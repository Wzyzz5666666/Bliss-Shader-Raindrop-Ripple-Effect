The block rendering distance must not be lower than the shadow rendering distance, otherwise the latter is invalid.

It's recommended to use 'Settings.txt' for the light and shadow configuration file.
